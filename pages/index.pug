extends common/pug/_layout.pug

block links
    link(rel="stylesheet", href="css/index.css")

block content
    include sections/index/_main.pug
    include sections/index/_stars.pug
    include sections/index/_news.pug
    include sections/index/_stack.pug
    include sections/index/_about.pug

    script.
        header = document.querySelector('header')
        header.classList.add('transparent')

        window.addEventListener('scroll', event => {
            this.scrollY > 0 ? header.classList.remove("transparent") : header.classList.add("transparent")
        })